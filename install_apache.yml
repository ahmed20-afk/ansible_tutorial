---

- hosts: all
  become: true
  tasks:


    - name: update repository index
      dnf:
        update_cache: yes


    - name: install apache2 package
      dnf:
        name: httpd
        state: latest

    - name: install php for apache2
      dnf:
        name: php.x86_64
        state: latest
